トップページ
<div id="slider">
  <% @movies.each do |movie| %>
    <%= link_to movie_path(movie) do %>
      <%= attachment_image_tag movie, :image, :fill, 600, 400, format: "jpeg" %>
    <% end %>
   <% end %>
</div>


<script>
$(function() {
  $('#slider').not('.slick-initialized').slick({
      dots: true, //スライドの下にドットのナビゲーションを表示
      autoplay: true, //自動再生
      autoplaySpeed: 4000, //再生スピード
  });
  console.log("slick!!!")
});
</script>

<%= link_to 'ジャンル一覧', genres_path %>